# Examples

This directory holds educational or experimental code that is not part of the main training/inference pipeline. The `transformers/gpt_transformer.py` script is a minimal NumPy-based transformer example.
